<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Ảnh thú cưng -->
        <ImageView
            android:id="@+id/imgPet"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:scaleType="centerCrop"
            android:src="@drawable/sample_dog"
            android:background="@drawable/bg_rounded_image"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Tên -->
        <TextView
            android:id="@+id/txtName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Max"
            android:textStyle="bold"
            android:textSize="16sp"
            android:textColor="#000"
            app:layout_constraintStart_toEndOf="@id/imgPet"
            app:layout_constraintTop_toTopOf="@id/imgPet"
            app:layout_constraintEnd_toStartOf="@id/txtStatus"
            android:layout_marginStart="12dp"/>

        <!-- Badge trạng thái -->
        <TextView
            android:id="@+id/txtStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Khỏe mạnh"
            android:textSize="12sp"
            android:textColor="#FFFFFF"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:background="@drawable/bg_status_green"
            app:layout_constraintTop_toTopOf="@id/txtName"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Giống + tuổi -->
        <TextView
            android:id="@+id/txtBreedAge"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Golden Retriever • 5 tuổi"
            android:textSize="13sp"
            android:textColor="#666"
            app:layout_constraintStart_toStartOf="@id/txtName"
            app:layout_constraintTop_toBottomOf="@id/txtName"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="4dp"/>

        <!-- Cân nặng + màu -->
        <TextView
            android:id="@+id/txtInfo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Cân nặng: 32 kg   Màu: Vàng"
            android:textSize="13sp"
            android:textColor="#666"
            app:layout_constraintStart_toStartOf="@id/txtName"
            app:layout_constraintTop_toBottomOf="@id/txtBreedAge"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="2dp"/>

        <!-- Link hoạt động -->
        <TextView
            android:id="@+id/txtActivity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="3 nhắc nhở đang hoạt động"
            android:textSize="13sp"
            android:textColor="#1E88E5"
            app:layout_constraintStart_toStartOf="@id/txtName"
            app:layout_constraintTop_toBottomOf="@id/txtInfo"
            android:layout_marginTop="4dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>